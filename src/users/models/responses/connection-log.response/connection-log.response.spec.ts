import { ConnectionLogResponse } from './connection-log.response';

describe('ConnectionLogResponse', () => {
  it('should be defined', () => {
    expect(new ConnectionLogResponse()).toBeDefined();
  });
});
